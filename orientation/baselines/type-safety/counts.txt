ShipShape Audit — Type Safety Baseline
Generated: 2026-05-19
Scope: web/src, api/src, shared/src, e2e (.ts/.tsx)
Tool: ripgrep 14.1.1

NOTE on tsc-baseline.txt:
  `pnpm type-check` and direct `tsc --noEmit` invocations are blocked by the
  audit sandbox (Bash denies pnpm/tsc/node). The compile-error count therefore
  could not be captured dynamically. All other metrics below are derived from
  static text scans.

================================================================
1) EXPLICIT ANY (`: any` colon-form, type position)
================================================================
Method:  rg -n ' any' --type ts <dir>   (raw)
         then  grep -c ': any' <raw>     (strict colon form)

Per-package strict `: any` count:
  web/src      24
  api/src      75
  shared/src    0
  e2e           4
  TOTAL       103

(For context, raw " any" matches: web=64, api=276, shared=1, e2e=50.
 The extra raw matches include `as any`, `: any[]` substring already
 captured, comments such as "as any other field", and prose.)

================================================================
2) TYPE ASSERTIONS (`as Type`, excluding `as const` and `import…as`)
================================================================
Method:  rg -n ' as ' --type ts <dir>                 → *_all_as.txt
         grep -v 'as const' AND grep -v 'import .* as' (filter)
         grep -cE 'as ([A-Z][a-zA-Z_0-9]*|any|unknown|string|number|boolean|never|void)'

Per-package real TS type-assertion count:
  web/src     267
  api/src     288
  shared/src    0
  e2e          22
  TOTAL       577

Sub-breakdown of `as any` (worst-case casts):
  web/src       7
  api/src     151   ← dominated by Vitest mock setup in *.test.ts
  shared/src    0
  e2e           2
  TOTAL       160

`as unknown` (intermediate widening):
  api/src       9    (sample only — full counts not separately tabulated)

Raw ' as ' line counts (includes SQL `column AS alias` inside template
literals, comments, English prose) — for reference / presearch reconciliation:
  web/src     453
  api/src     964   ← inflated by raw SQL "column AS alias" in route SQL
  shared/src    5
  e2e         124
  TOTAL     1,546   (the presearch estimate ~1,474 falls in this range)

================================================================
3) NON-NULL ASSERTIONS (`!`)
================================================================
Method:  rg -n '!\.', '!\[', '!\)' --type ts <dir>
         then manual sample to discount false positives (`![ ` for `!['x']`,
         `(x != y)` patterns not captured by these three suffixes).

Raw counts (per pattern × package):
                  !.   ![   !)
  web/src         17    1   10
  api/src          7    9   13
  shared/src       0    0    0
  e2e             11    0    5
  ─────────────────────────────
  RAW TOTAL       35   10   28   = 73

Manual de-duplication (after sampling, removing prose / `![` false hits
from `if (![…].includes(x))` and similar):
  web/src     ≈ 28
  api/src     ≈ 22
  shared/src    0
  e2e         ≈ 16
  TOTAL       ≈ 66

================================================================
4) @ts-ignore / @ts-expect-error
================================================================
Method:  rg -nF '@ts-ignore' / '@ts-expect-error' --type ts <all dirs>

  @ts-ignore         0
  @ts-expect-error   1
    (web/src/components/icons/uswds/Icon.test.tsx:63 — legitimate negative test)

================================================================
5) TSCONFIG SAFETY SETTINGS
================================================================
Root tsconfig.json (/Users/sheep/Desktop/Gauntlet/ship/tsconfig.json):
  strict:                       true
  noUncheckedIndexedAccess:     true
  noImplicitReturns:            true
  noFallthroughCasesInSwitch:   true
  exactOptionalPropertyTypes:   (not set — defaults to false)

api/tsconfig.json — extends root → inherits all safety flags.
shared/tsconfig.json — extends root → inherits all safety flags.

web/tsconfig.json — DOES NOT EXTEND root.
  strict:                       true   (locally set)
  noUncheckedIndexedAccess:     MISSING (default false)
  noImplicitReturns:            MISSING (default false)
  noFallthroughCasesInSwitch:   MISSING (default false)
  exactOptionalPropertyTypes:   MISSING (default false)

  ⚠ This means React/TS code in web/ silently allows:
    - reading possibly-undefined array elements without narrowing
    - functions that conditionally return (missing return on some branches)
    - switch fallthrough without explicit break/comment

================================================================
6) TOP VIOLATION-DENSE FILES (real assertions, before adding ` : any`)
================================================================
Generated by extracting filename column from real-assertion lines and
counting via sort | uniq -c | sort -rn.

API top (real `as`-style assertions):
  34  api/src/services/accountability.test.ts          (vi.mocked mocks as any)
  33  api/src/__tests__/auth.test.ts                   (mock fixtures)
  28  api/src/__tests__/transformIssueLinks.test.ts    (mock calls[][])
  25  api/src/routes/weeks.ts                          (id as string, body casts)
  23  api/src/__tests__/activity.test.ts               (mock.calls[][])
  20  api/src/routes/issues-history.test.ts
  17  api/src/routes/projects.test.ts
  12  api/src/routes/admin.ts
  10  api/src/routes/projects.ts
  10  api/src/mcp/server.ts

WEB top:
  29  web/src/pages/UnifiedDocumentPage.tsx            (document.properties as X)
  24  web/src/components/UnifiedEditor.tsx
  17  web/src/components/document-tabs/ProjectDetailsTab.tsx
  16  web/src/components/sidebars/PropertiesPanel.tsx
   9  web/src/hooks/useWeeklyReviewActions.ts
   9  web/src/components/document-tabs/ProgramOverviewTab.tsx
   8  web/src/hooks/useWeeksQuery.ts
   8  web/src/hooks/useIssuesQuery.ts
   8  web/src/components/editor/CommentDisplay.tsx
   8  web/src/components/document-tabs/WeekOverviewTab.tsx

================================================================
7) GRAND TOTAL (combined violation count)
================================================================
Strict `: any`                103
Type assertions (real)        577
Non-null assertions (~)        66
@ts-ignore                      0
@ts-expect-error                1
────────────────────────────────
TOTAL                         747

(Tighter bound; the looser bound that matches the presearch report
language is ~1,546 if you include SQL "AS" aliases inside SQL strings,
which is not actually a TypeScript safety concern.)

================================================================
8) RAW DATA FILES PRESERVED IN THIS DIRECTORY
================================================================
  *_any_raw.txt              — full rg ' any' matches per package
  *_all_as.txt               — full rg ' as ' matches per package
  *_assertions_strict.txt    — minus 'as const' / 'import…as'
  *_no_imports.txt           — used for real-assertion filtering
  *_real_assertions.txt      — only `as TYPE` real type assertions
  *_real_perfile.txt         — count-per-file (uniq -c output)
  *_real_top.txt             — sorted desc by count
  *_nn_dot.txt, *_nn_bracket.txt, *_nn_paren.txt — non-null pattern matches
  ts_ignore.txt, ts_expect_error.txt — directive matches
  sample_unified.txt, sample_weeks.txt — illustrative samples
