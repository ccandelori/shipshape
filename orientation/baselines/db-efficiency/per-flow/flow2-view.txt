  0.359ms	(prepared stmt — SQL same as prior)
  0.196ms	(prepared stmt — SQL same as prior)
  0.125ms	(prepared stmt — SQL same as prior)
  0.052ms	SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
